<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JulhoCamisas.CadastrarFornecedor"
             Shell.NavBarIsVisible="False">
    <ScrollView>
        <StackLayout Padding="30" Spacing="20">
            <Image Source="logo.png" HeightRequest="100" VerticalOptions="CenterAndExpand" />
            <Label Text="CADASTRAR FORNECEDOR" FontSize="24" HorizontalOptions="Center" />
            <StackLayout Spacing="10">
                <Entry Placeholder="Nome:" x:Name="nameEntry"/>
                <Entry Placeholder="Email:" x:Name="emailEntry"/>
                <Entry Placeholder="Endereço:" x:Name="addressEntry"/>
                <Entry Placeholder="Produto:" x:Name="productEntry"/>
                <Entry Placeholder="Qt:" x:Name="quantityEntry"/>
            </StackLayout>
            <Button Text="CONFIRMAR DADOS" Clicked="OnConfirmDataClicked" />
            <Button Text="VOLTAR" Clicked="OnBackClicked" />
        </StackLayout>
    </ScrollView>
</ContentPage>
